# Database connection
# Update with your local PostgreSQL credentials
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/local_music?schema=public"

# For PostGIS support, ensure your PostgreSQL has the PostGIS extension installed
# CREATE EXTENSION IF NOT EXISTS postgis;

# Redis (for production)
REDIS_URL="redis://localhost:6379"

# AI Provider API Keys (for AI features)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_AI_API_KEY=
DEEPSEEK_API_KEY=

# Google Maps API Key (for geocoding/maps)
GOOGLE_MAPS_API_KEY=

# Slack webhook for notifications (scraper approvals, parser failures)
SLACK_WEBHOOK_URL=

# Production only
# POSTGRES_PASSWORD=your_secure_password
# DOMAIN=yourdomain.com
